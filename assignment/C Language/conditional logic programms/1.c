#include <stdio.h>
int main()
{
    int a,b;
    printf("Enter A and B value here :");
    scanf("%d%d",&a,&b);
    if(a==b)
    {
        printf("A and B are equal!!");
    }
    else
    {
        printf("A and B are not equal!!");
    }
}
